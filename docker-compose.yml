version: '3.7'

services:
    dev:
        build:
            context: .
            dockerfile: Dockerfile.dev
        container_name: api-dev
        environment:
            - PORT=$PORT
        volumes:
            - .:/app
        ports:
            - '$PORT:$PORT'
        command: npm run dev:docker
