version: '3.7'

services:
    test:
        build:
            context: .
            dockerfile: Dockerfile.dev
        container_name: api-test
        environment:
            - PORT=3001
        volumes:
            - ./src:/app/src
            - ./tests:/app/tests
        ports:
            - '$PORT:3000'
        command: npm run test
